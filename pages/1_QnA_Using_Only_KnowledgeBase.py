import boto3
import streamlit as st
from dotenv import load_dotenv
import os

load_dotenv()

st.subheader('RAG Using Knowledge Base from Amazon Bedrock', divider='rainbow')
st.markdown('WIP - This is a simple RAG application that uses Amazon Bedrock Knowledge Base to answer questions. The Knowledge Base is created using Amazon Bedrock Knowledge Base and the data is uploaded to Amazon Bedrock Knowledge Base. The Knowledge Base is then used to answer questions using Amazon Bedrock Knowledge Base.')
